#Сортиране

#include<stdio.h>
int main ()
{ 
int a[1000];
int n, i;
do{scanf(%d”, &n);}
while(n<1||n>1000)
for(i=0;  i<n;  i++)
{
  scanf(“%d”, &a[i]);
if(a[i]=42){ max=I; break;}
}
for(i=0; i<max-1; i++)
{
min=a[i]
for(b=i+1; b<n; b++)
{
if(a[b]<min) min=a[k];
b=min;
min=a[i];
a[i]=b;
}
for(i=0; i<max; i++) printf(“%d”, a[i]);
return 0;
}
}
